import { z } from 'zod';

export const loginSchema = z.object({
  // TODO: Define login schema fields
});

export const registerSchema = z.object({
  // TODO: Define register schema fields
});

export const refreshTokenSchema = z.object({
  // TODO: Define refresh token schema fields
});
